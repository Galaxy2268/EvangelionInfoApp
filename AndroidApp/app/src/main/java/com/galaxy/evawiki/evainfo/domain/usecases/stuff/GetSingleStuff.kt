package com.galaxy.evawiki.evainfo.domain.usecases.stuff

import com.galaxy.evawiki.evainfo.domain.repository.StuffRepository

class GetSingleStuff(
    private val stuffRepository: StuffRepository
) {
}